package com.example.demo.competitive_programming

fun main() {
    val n = readLine()!!.toInt()
    val list = readLine()!!.split(" ").map(String::toInt)
    val sortedList = list.sortedDescending()
    (0..n).sorted().forEachIndexed { index, i ->  }

}